import common from './common';
import cases from './cases';
import product from "./product";
import config from "./api_config";
import home from '../pages/home/model';

export default [common, cases, config, product, home, ];
